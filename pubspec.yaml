name: indian_number_utils
description: Utilities for formatting and converting numbers using the Indian numbering system (lakh/crore, compact, words).
version: 0.1.0
homepage: https://github.com/priyeshchowta/indian_number_utils

environment:
  sdk: ">=2.18.0 <4.0.0"

# dependencies:
  # no runtime deps needed for now

dev_dependencies:
  flutter_test:
    sdk: flutter
  lints: ^6.0.0
